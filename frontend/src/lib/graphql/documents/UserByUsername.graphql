query UserByUsername($username: String!) {
  userByUsername(username: $username) {
    id
    username
    displayName
    bio
    avatarUrl
    tweetsCount
    followersCount
    followingCount
    isFollowedByMe
    createdAt
  }
}
