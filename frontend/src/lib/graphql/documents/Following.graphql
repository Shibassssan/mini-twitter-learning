query Following($uuid: ID!, $first: Int, $after: String) {
  following(uuid: $uuid, first: $first, after: $after) {
    edges {
      node {
        id
        username
        displayName
        bio
        avatarUrl
        isFollowedByMe
      }
      cursor
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
