# Local development defaults.
#
# Rails does not load this file automatically in the current setup.
# To override defaults, copy to .env and source it before starting Rails:
#   cp .env.example .env
#   set -a; source .env; set +a

DATABASE_URL=postgresql://postgres:password@localhost:5432/mini_twitter_development
TEST_DATABASE_URL=postgresql://postgres:password@localhost:5432/mini_twitter_test
REDIS_URL=redis://localhost:6379/0
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional in development. Generate with:
#   bin/rails secret
SECRET_KEY_BASE=
